<Window x:Class="Buoi05.BT1.View.StudentView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Buoi05.BT1.ViewModel"
        mc:Ignorable="d"
        Title="StudentView" Height="450" Width="800">
    <Window.DataContext>
        <local:StudentViewModel/>
    </Window.DataContext>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Danh sách sinh viên" HorizontalAlignment="Center" Foreground="#7898cc" FontWeight="bold" FontSize="25"/>
        <StackPanel Margin="10,0,0,10" Grid.Row="1" Orientation="Horizontal">
            <TextBlock Text="Họ và tên" FontWeight="Bold" FontSize="16" Width="200"/>
            <TextBlock Text="Tuổi" FontWeight="Bold" FontSize="16" Width="50"/>
        </StackPanel>
        <Border Grid.Row="2" BorderThickness="3">
            <ListBox ItemsSource="{Binding Students}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#CCC" BorderThickness="1" CornerRadius="3" Margin="2" Padding="5">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding Name}" Width="200" FontSize="16"/>
                                <TextBlock Text="{Binding Age}" Width="200" FontSize="16"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>
    </Grid>
</Window>
