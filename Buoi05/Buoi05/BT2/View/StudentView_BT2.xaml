<Window x:Class="Buoi05.BT2.View.StudentView_BT2"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Buoi05.BT2.ViewModel"
        mc:Ignorable="d"
        Title="StudentView_BT2" Height="450" Width="800">
    <Window.DataContext>
        <local:StudentVM_BT2/>
    </Window.DataContext>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Danh sách sinh viên" Foreground="CadetBlue" FontSize="25" FontWeight="Bold" HorizontalAlignment="Center"/>
        <Grid Margin="10" Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="100"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBlock Text="Họ và tên" FontWeight="Bold" />
                <TextBox x:Name="txtName" Text="{Binding Name, Mode=TwoWay}"/>
            </StackPanel>
            <StackPanel Grid.Column="1" Margin="10,0,0,0">
                <TextBlock Text="Tuổi" FontWeight="Bold"/>
                <TextBox x:Name="txtAge" Text="{Binding Age, Mode=TwoWay}"/>
            </StackPanel>
        </Grid>
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="10,0,0,0">
            <Button Content="Thêm" Width="150" Margin="0,0,5,0" Click="BtnAdd_Click"/>
            <Button Content="Xóa" Width="150" Click="BtnDel_Click"/>
        </StackPanel>
        <Border Grid.Row="3" BorderThickness="2">
            <ListBox Margin="7,0,7,0" ItemsSource="{Binding Students}" SelectedItem="{Binding SelectedStudent}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Width="200" Text="{Binding Name}"/>
                            <TextBlock Width="50" Text="{Binding Age}"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>
    </Grid>
</Window>
