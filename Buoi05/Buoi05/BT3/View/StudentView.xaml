<Window x:Class="Buoi05.BT3.View.StudentView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Buoi05.BT3.ViewModel"
        mc:Ignorable="d"
        Title="StudentView" Height="450" Width="950">
    <Window.DataContext>
        <local:StudentVM_Ob/>
    </Window.DataContext>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Danh sách sinh viên" FontSize="30" HorizontalAlignment="Center" FontWeight="Bold" Margin="10"/>
        <StackPanel Orientation="Horizontal" Grid.Row="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Họ và tên" FontSize="16" FontWeight="Bold"/>
                <TextBox Grid.Row="1" Width="200" Text="{Binding NewName, Mode=TwoWay}"/>
                <TextBlock Grid.Column="1" Text="Tuổi" FontSize="16" FontWeight="Bold" Margin="10,0,-10,0" />
                <TextBox Grid.Column="1" Width="60" Margin="10,21,0,0" Grid.RowSpan="2" Text="{Binding NewAge, Mode=TwoWay}"/>
            </Grid>
            <Button Content="Thêm" Width="100" Margin="10,0,0,0 " FontSize="16" Click="BtnAdd_Click"/>
            <Button Content="Xóa" Width="100" Margin="10,0,0,0 " FontSize="16" Click="BtnDel_Click"/>
            <TextBlock Text="Tìm kiếm: " FontSize="16" VerticalAlignment="Center" Margin="10,0,0,0 "/>
            <TextBox Width="200" Text="{Binding FilterText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            <Button Content="Sắp xếp tuổi" Width="100" Margin="10,0,0,0 " FontSize="16" Click="BtnSort_Click"/>
        </StackPanel>
        <Border Grid.Row="2" BorderThickness="2" Margin="0,10,0,0">
            <ListBox ItemsSource="{Binding StudentView}" SelectedItem="{Binding SeclectionStudent}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Width="200" Text="{Binding Name}"/>
                            <TextBlock Width="50" Text="{Binding Age}"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>
    </Grid>
</Window>
