<Window x:Class="KTL2.Views.Bai3View"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:KTL2.ViewModels"
        mc:Ignorable="d"
        Title="frmDatPhong"
        Height="520"
        Width="850"
        WindowStartupLocation="CenterScreen"
        Background="LightGray">

    <Window.DataContext>
        <vm:Bai3VM/>
    </Window.DataContext>

    <Grid Margin="15">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="360"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <GroupBox Header="Thông tin đặt phòng"
                  Grid.Column="0"
                  Margin="0,0,10,0"
                  Padding="15">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="25"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="110"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="Ngày đặt phòng"
                           Grid.Row="0"
                           Grid.Column="0"
                           VerticalAlignment="Center"
                           Margin="0,5"/>

                <DatePicker Grid.Row="0"
                            Grid.Column="1"
                            SelectedDate="{Binding NgayDat}"
                            Height="28"
                            Margin="0,5"/>

                <TextBlock Text="Chọn phòng"
                           Grid.Row="1"
                           Grid.Column="0"
                           VerticalAlignment="Center"
                           Margin="0,5"/>

                <ComboBox Grid.Row="1"
                          Grid.Column="1"
                          ItemsSource="{Binding Ds_Phong}"
                          DisplayMemberPath="TenPhong"
                          SelectedItem="{Binding SelectedPhong}"
                          Height="28"
                          Margin="0,5"/>

                <TextBlock Text="Giá phòng"
                           Grid.Row="2"
                           Grid.Column="0"
                           VerticalAlignment="Center"
                           Margin="0,5"/>

                <TextBox Grid.Row="2"
                         Grid.Column="1"
                         Text="{Binding GiaPhong}"
                         IsReadOnly="True"
                         Height="28"
                         Margin="0,5"/>

                <TextBlock Text="Sức chứa"
                           Grid.Row="3"
                           Grid.Column="0"
                           VerticalAlignment="Center"
                           Margin="0,5"/>

                <TextBox Grid.Row="3"
                         Grid.Column="1"
                         Text="{Binding SucChua}"
                         IsReadOnly="True"
                         Height="28"
                         Margin="0,5"/>

                <TextBlock Text="Chọn khách hàng"
                           Grid.Row="5"
                           Grid.Column="0"
                           VerticalAlignment="Center"
                           Margin="0,5"/>

                <ComboBox Grid.Row="5"
                          Grid.Column="1"
                          ItemsSource="{Binding Ds_KhachHang}"
                          DisplayMemberPath="TenKH"
                          SelectedItem="{Binding SelectedKhachHang}"
                          Height="28"
                          Margin="0,5"/>

                <TextBlock Text="Giờ vào"
                           Grid.Row="6"
                           Grid.Column="0"
                           VerticalAlignment="Center"
                           Margin="0,5"/>

                <TextBox Grid.Row="6"
                         Grid.Column="1"
                         Text="{Binding GioVao, UpdateSourceTrigger=PropertyChanged}"
                         Height="28"
                         Margin="0,5"/>

                <TextBlock Text="Giờ ra"
                           Grid.Row="7"
                           Grid.Column="0"
                           VerticalAlignment="Center"
                           Margin="0,5"/>

                <TextBox Grid.Row="7"
                         Grid.Column="1"
                         Text="{Binding GioRa, UpdateSourceTrigger=PropertyChanged}"
                         Height="28"
                         Margin="0,5"/>
            </Grid>
        </GroupBox>

        <GroupBox Header="Thông tin phụ thu"
                  Grid.Column="1"
                  Padding="15">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="70"/>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="70"/>
                        <ColumnDefinition Width="80"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="Chọn phụ thu"
                               Grid.Column="0"
                               VerticalAlignment="Center"/>

                    <ComboBox Grid.Column="1"
                              ItemsSource="{Binding Ds_PhuThu}"
                              DisplayMemberPath="TenPhuThu"
                              SelectedItem="{Binding SelectedPhuThu}"
                              Height="28"
                              Margin="0,0,10,0"/>

                    <TextBlock Text="Giá"
                               Grid.Column="2"
                               VerticalAlignment="Center"/>

                    <TextBox Grid.Column="3"
                             Text="{Binding GiaPhuThu}"
                             IsReadOnly="True"
                             Height="28"
                             Margin="0,0,10,0"/>

                    <Button Content="Thêm"
                            Grid.Column="5"
                            Command="{Binding ThemPhuThuCommand}"
                            Height="28"/>
                </Grid>

                <Grid Grid.Row="1" Margin="0,12,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="150"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="Số lượng"
                               Grid.Column="0"
                               VerticalAlignment="Center"/>

                    <TextBox Grid.Column="1"
                             Text="{Binding SoLuong, UpdateSourceTrigger=PropertyChanged}"
                             Height="28"/>
                </Grid>

                <DataGrid Grid.Row="2"
                          ItemsSource="{Binding Ds_ChiTietPhuThu}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          IsReadOnly="True"
                          Margin="0,5,0,10">

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Mã phụ thu"
                                            Binding="{Binding MaPT}"
                                            Width="90"/>

                        <DataGridTextColumn Header="Tên phụ thu"
                                            Binding="{Binding TenPhuThu}"
                                            Width="*"/>

                        <DataGridTextColumn Header="Giá phụ thu"
                                            Binding="{Binding GiaPT}"
                                            Width="100"/>

                        <DataGridTextColumn Header="Số lượng"
                                            Binding="{Binding SL}"
                                            Width="80"/>

                        <DataGridTextColumn Header="Thành tiền"
                                            Binding="{Binding ThanhTien}"
                                            Width="100"/>
                    </DataGrid.Columns>
                </DataGrid>

                <Grid Grid.Row="3" Margin="0,5,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="130"/>
                        <ColumnDefinition Width="150"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="Tổng tiền tạm tính"
                               Grid.Column="1"
                               VerticalAlignment="Center"
                               HorizontalAlignment="Right"
                               Margin="0,0,10,0"/>

                    <TextBox Grid.Column="2"
                             Text="{Binding TongTien}"
                             IsReadOnly="True"
                             Height="28"/>
                </Grid>

                <Button Grid.Row="4"
                        Content="Đặt phòng"
                        Command="{Binding DatPhongCommand}"
                        Width="100"
                        Height="32"
                        HorizontalAlignment="Center"/>
            </Grid>
        </GroupBox>
    </Grid>
</Window>