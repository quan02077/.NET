<UserControl x:Class="Buoi11.BT4.ucMonHoc"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Buoi11.BT4"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <local:MonHocVM/>
    </UserControl.DataContext>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Quản lý Môn học" FontWeight="Bold" FontSize="22" Margin="0,0,0,25"/>

        <Grid Grid.Row="1" Margin="0,0,0,25">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="Mã môn:" VerticalAlignment="Center" Margin="0,0,15,15"/>
            <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding MaMon}" Margin="0,0,40,15" Padding="4" Height="28"/>

            <TextBlock Grid.Row="0" Grid.Column="2" Text="Tên môn học:" VerticalAlignment="Center" Margin="0,0,15,15"/>
            <TextBox Grid.Row="0" Grid.Column="3" Text="{Binding TenMH}" Margin="0,0,0,15" Padding="4" Height="28"/>

            <TextBlock Grid.Row="1" Grid.Column="0" Text="Số TC:" VerticalAlignment="Center" Margin="0,0,15,0"/>
            <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding SoTC}" Margin="0,0,40,0" Padding="4" Height="28"/>

            <TextBlock Grid.Row="1" Grid.Column="2" Text="Tính chất:" VerticalAlignment="Center" Margin="0,0,15,0"/>
            <ComboBox Grid.Row="1" Grid.Column="3" Text="{Binding TinhChat}" Padding="4" Height="28">
                <ComboBoxItem Content="Tự chọn"/>
                <ComboBoxItem Content="Bắt buộc"/>
            </ComboBox>
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,25">
            <Button Content="Thêm" Command="{Binding ThemCmd}" Width="85" Height="32" Margin="0,0,20,0" Background="#E6E6E6"/>
            <Button Content="Sửa" Command="{Binding SuaCmd}" Width="85" Height="32" Margin="0,0,20,0" Background="#E6E6E6"/>
            <Button Content="Xóa" Command="{Binding XoaCmd}" Width="85" Height="32" Margin="0,0,20,0" Background="#E6E6E6"/>
            <Button Content="Lưu" Command="{Binding LuuCmd}" Width="85" Height="32" Margin="0,0,20,0" Background="#E6E6E6"/>
            <Button Content="Hủy" Command="{Binding HuyCmd}" Width="85" Height="32" Background="#B3E5FC"/>
        </StackPanel>

        <DataGrid Grid.Row="3" ItemsSource="{Binding DS_MonHoc}" SelectedItem="{Binding SelectedMonHoc}" 
              AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" 
              HeadersVisibility="Column" Background="#F0F0F0" AlternatingRowBackground="#FAFAFA">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Mã môn" Binding="{Binding MaMonHoc}" Width="*"/>
                <DataGridTextColumn Header="Tên môn học" Binding="{Binding TenMonHoc}" Width="2*"/>
                <DataGridTextColumn Header="Số TC" Binding="{Binding SoTC}" Width="*"/>
                <DataGridTextColumn Header="Tính chất" Binding="{Binding TinhChat}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
