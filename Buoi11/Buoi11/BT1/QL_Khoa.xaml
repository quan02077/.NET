<Window x:Class="Buoi11.BT1.QL_Khoa"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Buoi11.BT1"
        xmlns:vm="clr-namespace:Buoi11.BT1"
        mc:Ignorable="d"
        Title="QL_Khoa" Height="450" Width="800">
    <Window.DataContext>
        <vm:KhoaVM />
    </Window.DataContext>
    <Grid Margin="15">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1.5*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Margin="0,0,15,0">
            <TextBlock Text="THÔNG TIN KHOA" FontWeight="Bold" FontSize="14" Margin="0,0,0,15" Foreground="DimGray"/>

            <TextBlock Text="Mã khoa:"/>
            <TextBox Text="{Binding NewKhoa.MaKhoa,
                UpdateSourceTrigger=PropertyChanged,
                ValidatesOnDataErrors=True,
                NotifyOnValidationError=True}"
                ToolTip="{Binding RelativeSource={RelativeSource Self},
                Path=(Validation.Errors)[0].ErrorContent}" />
            <TextBlock Text="Tên khoa:"/>
            <TextBox Text="{Binding NewKhoa.TenKhoa, 
             UpdateSourceTrigger=PropertyChanged, 
             ValidatesOnDataErrors=True}" 
         Margin="0,5,0,20" Padding="2"
         ToolTip="{Binding RelativeSource={RelativeSource Self}, 
                           Path=(Validation.Errors)[0].ErrorContent}"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                <Button Content="Thêm" Command="{Binding ThemCmd}" Width="60" Margin="0,0,10,0" Padding="5"/>
                <Button Content="Sửa" Command="{Binding SuaCmd}" Width="60" Margin="0,0,10,0" Padding="5"/>
                <Button Content="Xóa" Command="{Binding XoaCmd}" Width="60" Padding="5"/>
            </StackPanel>
        </StackPanel>

        <StackPanel Grid.Column="1">
            <TextBlock Text="DANH SÁCH KHOA" FontWeight="Bold" FontSize="14" Margin="0,0,0,15" Foreground="DimGray"/>

            <DataGrid ItemsSource="{Binding DS_Khoa}" 
          SelectedItem="{Binding SelectedKhoa}" 
          AutoGenerateColumns="False" 
          CanUserAddRows="False"
          IsReadOnly="True" Height="250">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Mã khoa" Binding="{Binding MaKhoa}" Width="*"/>
                    <DataGridTextColumn Header="Tên khoa" Binding="{Binding TenKhoa}" Width="2*"/>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </Grid>
</Window>
